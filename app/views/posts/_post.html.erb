<div id="<%= dom_id post %>">
  
   <h3> <%= post.title %> </h3> 
   <h5> Posted by <%= post.user.email %> </h5>
   <h6> <%= pluralize(post.views, 'view') %> </h6>

  <div>    
    <%= post.body %>
  </div>

</div>
